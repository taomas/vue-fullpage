// var arr = ['hello', 'world']

// var result = arr.join('-')

// console.log(result)

var name = 'tony better'

var upper = name.split(' ')
  // .map((v) => { return v.charAt(0).toUpperCase() + v.slice(1)})
  // .join(' ')

console.log(upper)